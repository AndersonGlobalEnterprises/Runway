PORT=3000
NODE_ENV=development
SESSION_SECRET=generate-a-long-random-string-for-production
DEMO_LOGIN=true

# AI script generation + rewrite (required for in-portal editing)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# n8n pipeline — set false to queue topics locally only (no webhook)
N8N_QUEUE_ENABLED=true

# Creatomate video preview + final render (Video tab)
CREATOMATE_API_KEY=

# Social strategy — weekly flight plan (phase 2 after Core Edit)
PERPLEXITY_API_KEY=
